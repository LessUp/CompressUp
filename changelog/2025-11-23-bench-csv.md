# 2025-11-23 benchmark CSV 输出

- 为 `compressup_bench` 增加 `--csv <csv_file>` 可选参数：
  - 使用方式：`compressup_bench [--csv result.csv] <file1> [file2 ...]`。
  - 在保持终端表格输出的同时，将每条基准结果以 CSV 行写入指定文件，字段包括：
    - `algorithm,file,orig_bytes,comp_bytes,ratio,compress_ms,decompress_ms,compress_mb_s,decompress_mb_s`。
